<h2><%= @idea.title %></h2>

<p><%= link_to "Edit", edit_user_idea_path(@user, @idea) %> - <%= link_to "Delete", user_idea_path(@user, @idea), method: :delete %></p>

<p><%= @idea.category.name %></p>

<p><%= @idea.content %></p>

<% @idea.images.each do |image| %>
  <img src="<%= image.url %>">
<% end %>
